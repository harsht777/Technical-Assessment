events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location / {
            # Route traffic to the API container on port 5000
            proxy_pass http://api:5000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
